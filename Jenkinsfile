node {
  def go = tool 'go'

  try {
    sh 'echo $PATH'
    sh 'curl -O https://dl.google.com/go/go1.12.9.src.tar.gz'
    sh 'echo working'
    sh 'curl --version'
    sh 'pwd'
    sh 'go version'
  }
  catch(any){

  }
  finally{

  }
}
