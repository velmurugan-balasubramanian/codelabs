node {
  def go = tool 'go'

  try {
    sh 'echo $PATH'
    sh 'go get github.com/googlecodelabs/tools/claat'
    sh 'echo $GOPATH'
    sh 'echo working'
    sh 'curl --version'
    sh 'pwd'
    sh 'go version'
  }
  catch(any){

  }
  finally{

  }
}
