node {
  def go = tool 'go'

  try {
    sh 'echo $PATH'
    sh 'which go'
    sh 'echo $GOPATH'
    sh 'echo working'
    sh 'curl --version'
    sh 'pwd'
    sh 'go version'
  }
  catch(any){

  }
  finally{

  }
}
